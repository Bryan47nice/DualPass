rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 單人 app：只有本人（Google 登入）能讀寫任何文件
    match /{document=**} {
      allow read, write: if request.auth != null
        && request.auth.token.email == "bryan472017@gmail.com";
    }
  }
}
